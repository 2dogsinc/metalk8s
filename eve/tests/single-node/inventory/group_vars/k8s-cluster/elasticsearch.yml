---

custom_external_values:
  master:
    antiAffinity: "soft"
  data:
    antiAffinity: "soft"
    persistence:
      size: 4Gi

elasticsearch_external_values:
  - '{{ custom_external_values }}'
  - '{{ test_elasticsearch_external_values | default({})}}'
